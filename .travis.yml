language: php

os:
  - linux
  - osx

php:
  - 5.5
  - 5.6

before_install:
  - pip install --user codecov

before_script:
  - composer self-update
  - composer -q install
  - ./bin/post-update.sh

after_success:
  - cp var/phpunit/coverage.xml .
  - codecov

script:
  - ./bin/pre-commit.sh
